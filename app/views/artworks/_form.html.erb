<%= form_with(model: artwork) do |form| %>
  <% if artwork.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artwork.errors.count, "error") %> prohibited this artwork from being saved:</h2>

      <ul>
        <% artwork.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :prompt_id, style: "display: block" %>
    <%= form.collection_select(:artwork, :prompt_id, Prompt.all, :id, :text, prompt: true) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
